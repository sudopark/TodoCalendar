---
name: plan
description: Use when writing an implementation plan in this project — 킥오프 A-3 전환, /plan 명시 호출, superpowers:writing-plans invoke 등 플랜 문서를 작성하는 모든 시점. writing-plans를 대체하지 않는 컴패니언 — 산출 플랜이 다른 세션이 플랜만 보고 착수 가능한 인수인계 문서가 되도록 프로젝트 종속 요구(스코프 경계, 커밋 시퀀스 사전 계획, 태스크별 모델 티어, 범위 명확성 전제, 자족성 체크)를 주입한다. Triggers on "플랜 짜자", "계획 작성하자", writing-plans 스킬 사용 시 함께. Does NOT trigger on 플랜 실행·이슈 분해·설계 논의만 할 때.
---

# Plan — TodoCalendar 플랜 작성 컴패니언

**superpowers:writing-plans의 대체가 아니다.** 문서 구조·태스크 분해·TDD 스텝·No Placeholders 원칙은 writing-plans가 이끈다. 이 스킬은 산출 플랜이 **다른 세션 에이전트가 플랜만 보고 착수 가능한 인수인계 문서**가 되도록 프로젝트 종속 요구를 추가한다. 실행 에이전트는 이 세션의 대화·탐색 기억 없이, 플랜에 적힌 것만 갖고 시작한다.

## 전제 — 범위 명확성

플랜은 **후속 이슈 분리가 필요 없을 정도로 확실한 영역**에서 작성한다. 상류(이슈 분해·킥오프 충분성 게이트)에서 경계가 깔끔하게 잘렸다는 전제다.

작성 중 이 전제가 깨지면 — 태스크를 확정할 수 없거나, 스코프 경계가 안 그어지거나, "일단 해보고 판단" 스텝이 필요해지면 — **플랜을 계속 쓰지 말고 멈춰서 유저와 해소한다.** 킥오프 게이트(반문)로 되돌아가는 게 정상 경로다. 범위가 흐린 플랜을 실행자에게 넘기면 우발상황 반문이 빈발한다.

## 작업 유형별 결정 포인트 게이트

### 신규 설정·선택 UI

새 설정 화면·선택/편집 UI(위젯 편집 시트·AppIntents 파라미터 UI 포함)를 만드는 태스크가 있으면, 아래 넷이 스펙·플랜에 결정돼 있는지 확인한다. 하나라도 미정이면 플랜을 멈추고 킥오프 게이트(반문)로 되돌린다 — 넷 다 구현 분기를 바꾼다:

- 목록의 **정렬 기준·포함/제외 범위** (예: 다가오는 순, 공휴일 포함 여부)
- 라벨·문구 **로컬라이즈**
- 항목·파라미터의 **노출·활성 조건** (예: 반복 일정일 때만 회차 노출)
- **아이콘이 기존 아이콘과 혼동되지 않는지**

### 코드 이동·모듈 분리의 계층 판별

타입·파일을 다른 모듈로 옮기는 플랜은 이동 대상마다 계층(usecase / service / engine)을 판별해 목록에 명시한다. "SDK import가 있다" 같은 표면 신호로 일괄 분류 금지 — usecase는 Domain 잔류(service 프로토콜 소비), service 구현체만 내려간다. 동형 선례(예: PlaceSuggest 분리 구조)를 찾아 대조하고 그 결과를 플랜에 적는다.

## 필수 섹션

writing-plans 기본 구조(Goal/Architecture/Global Constraints/태스크)에 다음 3개 섹션을 추가한다:

### 1. 스코프 경계 — 헤더 직후

- **건드릴 것 / 안 건드릴 것**을 파일·모듈 단위로 명시한다. 목표·수정 대상·수행 과정은 writing-plans 기본(Goal·Files·Steps)이 커버하므로, 이 섹션의 핵심 추가분은 **"안 건드릴 것"** — 실행자가 범위 밖으로 새는 것을 막는 선이다.

### 2. 커밋 시퀀스 — 태스크 정의 앞

- 최종 커밋 목록을 사전 계획한다: **논리 단위 묶음 + `[#이슈] 동작 변화 요약` 메시지 초안** (CLAUDE.md §5 컨벤션).
- **태스크 경계 ≠ 커밋 경계.** 여러 태스크가 한 커밋으로 묶일 수 있고, 그 대응을 시퀀스에 명시한다 (예: "커밋 2 = Task 2+3").
- 커밋은 결과 위주 — TDD 중간 상태(RED/GREEN 과정)를 커밋에 싣지 않는다. superpowers 태스크 템플릿의 `feat:` 커밋 스텝 대신, 각 태스크의 커밋 스텝이 이 시퀀스의 해당 항목을 참조하게 쓴다.

### 3. 태스크별 모델 티어·리뷰 게이트 — 커밋 시퀀스 뒤

실행 세션이 재판단 없이 그대로 dispatch할 수 있게, 태스크마다 모델 티어와 **태스크 리뷰 실행 여부**를 판정해 표로 명시한다:

- **기계적 transcription** — 플랜에 완전한 코드·내용이 있고 1-2 파일: **하위 모델 (haiku급)**
- **통합·판단** — 멀티 파일 조율, 기존 패턴 매칭, 프로즈 스펙에서 코드 도출: **표준 모델 (sonnet급)**
- **설계 판단** — 아키텍처 결정·광범위 코드베이스 이해 필요: **최상위 모델** (이런 태스크가 있다면 플랜이 덜 여문 신호 — 범위 명확성 전제 재점검)

**리뷰 게이트** — 판정 기준은 "이 태스크의 diff에서 리뷰어가 잡을 결함 종류가 테스트로는 안 잡히는가". 아니오면 생략 (SDD의 태스크 리뷰를 프로젝트 차원에서 선별 적용 — 토큰 절감):

- **리뷰 O**: 프로즈 스펙에서 코드를 도출, 멀티파일 통합, 동시성·마이그레이션·보안 리스크, 이후 태스크가 의존할 인터페이스 산출
- **리뷰 생략**: 플랜에 완전한 코드·내용이 있는 transcription, 기계적 목록 동기화·리소스 추가 등 테스트·스크립트가 검증을 대신하는 태스크. 최종 whole-branch 리뷰가 안전망으로 남는다. 실행 중 이상 신호 시 생략을 뒤집는 규칙은 implement 스킬 §착수가 규정한다.

## 자족성 체크 — 저장 전

플랜 저장 전 아래를 자문한다. 하나라도 "이 세션 기억에만 있다"면 플랜에 옮겨 적는다:

- **목표가 뭐고 뭐를 해야 하는지** — Goal + 태스크 목록으로 답해지는가
- **어디를 수정하고 어디까지가 범위인지** — Files + 스코프 경계로 답해지는가
- **어떤 과정으로 수행하고 어디서 커밋하는지** — 태스크 스텝 + 커밋 시퀀스로 답해지는가
- **탐색으로 알아낸 전제**(현재 동작 file:line, 함정, 오탐 알려진 것)가 Global Constraints나 태스크 본문에 적혀 있는가
- **실행자가 따라야 할 rules 조항**(테스트 작성이 있으면 testability.md의 더블·wait API 규칙 등)이 태스크 본문에 발췌돼 있는가 — 실행 서브에이전트는 path 매칭 자동 로드를 받지 못한다

## 실행 중 갭

플랜이 커버 못 하는 상황을 실행자가 만나면 implement 스킬의 **플랜 갭 보고 루프**를 탄다 (추측 금지·유저 반문·후속 정리). 이 스킬의 책임은 그 상황이 안 생기게 계획 단계에서 커버를 극대화하는 것까지다.

## 종료 기록 — skill_end (#715)

플랜 파일이 저장·승인돼 절차가 끝나면 skill_end를 기록한다 (명령·compliance 규칙은 CLAUDE.md §1). 이때 superpowers:writing-plans가 이 런에 실제 발동(invoke)됐으면 같은 시점에 함께 기록한다 — 발동 레코드와 동일한 `superpowers:` prefix 포함 이름으로. 플러그인 스킬은 자체 종료 조항이 없어 여기가 기록 주체다.

**superpowers:brainstorming도 같은 규칙으로 함께 기록한다** — 발산이 끝나고 방향이 플랜으로 확정된 시점이 그 종료다. 플랜을 안 거치고 바로 구현으로 간 런은 implement 스킬이 기록 주체다.
